---
type Props = { id: string };
const { id } = Astro.props;
---

<select {id} name={id}><slot /></select>

<style>
	select {
		font-size: 0.8em;
		font-family: var(--font-family);
		margin-left: var(--select-margin-left);
	}
</style>
